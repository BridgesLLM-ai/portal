{
  "schema": 1,
  "version": "4.0.5",
  "releasedAt": "2026-08-01",
  "releaseClass": "hotfix",
  "highlights": [
    "Updates no longer fail with 'the existing OpenClaw gateway is not stably ready' when the Portal restarts that gateway during its own startup.",
    "The update now waits for the gateway to finish restarting before judging whether it is healthy, instead of sampling it mid-restart.",
    "A gateway that has genuinely failed is still refused quickly, so the guard protecting your rollback baseline is unchanged."
  ]
}

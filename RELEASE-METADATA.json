{
  "schema": 1,
  "version": "4.0.16",
  "releasedAt": "2026-08-11",
  "releaseClass": "hotfix",
  "highlights": [
    "Updating works again on servers running 4.0.15, which refused to update because it did not recognize its own scheduled backup helper.",
    "The release build now fails if a release ships a backup helper its own installer would reject, so this cannot recur silently."
  ]
}
